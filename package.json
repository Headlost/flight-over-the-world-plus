{
  "name": "flight-over-the-world-plus",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "vite --host 127.0.0.1",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.js",
    "test:e2e": "playwright test",
    "prebuild": "npm test",
    "test:terrain-pool": "node --test server/n8n/*.test.js"
  },
  "engines": {
    "node": ">=22.13"
  },
  "dependencies": {
    "3d-tiles-renderer": "^0.5.2",
    "leaflet": "^1.9.4",
    "peerjs": "^1.5.5",
    "qrcode": "^1.5.4",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "vite": "^6.2.0"
  }
}
